{
  "name": "ai-doctor-backend",
  "version": "0.0.1",
  "scripts": {
    "dev": "python -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python -m uvicorn app:app --host 0.0.0.0 --port 8000"
  }
}
